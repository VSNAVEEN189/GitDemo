# having many forms". It is the ability to use a single interface (like a function name or an operator) 
# to perform different actions based on the type of object it's being used with
a = 10 
b = 20
print(a + b)  #Arthimetic addition
# Looks the same behaves differently
s1 = "Hello"
s2 = "Hi"
print(s1 + s2)  #Conctination of strings
