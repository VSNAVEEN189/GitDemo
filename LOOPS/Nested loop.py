# Nested loop are nothing but one loop inside another loop

for i in range(3):  #Outer for loop executes 3 times
    for j in range(2):  #Inner loop executes 2 times
        print(f"i={i}, j={j}")
# Depending on how many times the inner loop runs multiples with outer loop.

